FEATURES TO ADD:

-Piano Block:

  -Crafting:
  wooden slab = w
  string = s
  w w w
  s s s
  w w w

  -Using it: there is 3 strings (each is a note), you configure them and press play to play the notes

-Wizard armor/robes

//if(player.getGameProfile().getName().equals("paul325")) {    //maybe use get id?
//   // Give infinite nausea
//}

Working Notes:

-Removing files from github: add it to .gitignore, and do "git rm -r --cached .idea" in terminal, where .idea is the filename


